import prisma from "@repo/db/client";
import { getServerSession } from "next-auth";
import { authOptions } from "../auth";
interface:{
providers: string,
amount:number     
}

export   const createOnRampTransaction=({})=>{

} 